<%- include ('../partials/header.ejs') %>

<section>
    <div class="container">
    <br>


      <section>
        <div class="container">
            <h1 class="text-center niceColor">Welcome to my blog space𓍯𓂃𓏧♡</h1>
        </div>  
      </section>


    <br>

</div>

</section>

<section>
  <div class="container">
    <div class="row">
      <% blogs.forEach(blog => { %>
        <div class="col-md-3">
          <div class="card card-spacer">
            <img src="<%= blog.image %>" class="blogImage" alt="Blog image">
            
            <div class="card-body">
              <h5 class="card-title"><%= blog.title %></h5>
              <p class="card-text"><%= blog.content %></p>
              <p class="card-text"><small class="text-muted">By <%= blog.author || 'Anonymous' %> on <%= blog.createdAt.toDateString() %></small></p>
            </div>
            <!--<a class="btn btn-success" href="/blog/like/<%- blog.id %>">❤️</a>-->
          </div>
        </div>
      <% }) %>
    </div>
  </div>
</section>

<%- include ('../partials/footer.ejs') %>